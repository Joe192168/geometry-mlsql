#生产环境配置(配置项只包括不同项)
spring:
  datasource:
    url: jdbc:mysql://192.168.0.47:3306/mlsql_console2?useUnicode=true&characterEncoding=utf8&allowMultiQueries=true
    username: root
    password: ENC(NB2lV8hnECAOlNa5Dl+TVA==)
  redis:
    # 服务器IP地址
    host: biserver
    # 连接端口
    port: 6379
    # Redis服务器连接密码（默认为空）
    password:

#配置数字证书相关信息
license:
  licensePath: /license.lic
  subject: 项目名称
  publicAlias: publiccert
  storePass: abc123456
  publicKeysStorePath: /publiccerts.store

#白名单
ignore:
  urls:
    /api/**,
    /dealFilterChain/**,
    /biApi/**,
    /schema/**,
    /dataSource/**